

出一份完整的文档
agent回复中的图表可以放大（当然在wiki中的也是）
直接去掉rag，只保留agent模式，也删除agent有关的选择或者提示
 有空再继续优化wiki structure的生成
3. 需要仔细检查一下agent的逻辑 
4. 现在只有少量的file找不到（.github folder需要处理一下， 剩下的看起来是幻觉导致的）
9. license 不处理
整理vercel和fly的部署参数

对于小的项目，就不要embedding了，大的才embedding
使用合适的rank和embedding模型
调整rag变为agentic rag，配合证据卡片
路径那些东西，还是有点没搞懂

怎么选rank逻辑和embedding模型，优化一下使用的模型（可以用贵的）